# Optional — leave empty to process locally with the high-quality CPU fallback.
RUNPOD_API_KEY=
RUNPOD_ENDPOINT_ID=npjpz24ig6c47j

# Public URL of this app. Required for GPU jobs on files larger than ~8 MB
# so the worker can download the upload (e.g. https://enhance.example.com).
PUBLIC_BASE_URL=
